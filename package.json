{
  "name": "eaudoon-project-catalog",
  "private": false,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "validate": "node cli.mjs validate",
    "check": "node cli.mjs validate && node --test && node scripts/render.mjs --check && node scripts/links.mjs"
  }
}
