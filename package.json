{
  "name": "eaudoon-project-catalog",
  "private": false,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "validate": "node cli.mjs validate",
    "drift": "node scripts/drift.mjs --check",
    "check": "node cli.mjs validate && node --test && node scripts/render.mjs --check && node scripts/links.mjs && node scripts/drift.mjs --check"
  }
}
