# README Quick Start Audit (phase 15, 2026-09-23)

Verdict: added.

`README.md` opens with a GitHub-profile-style banner and a 30+ line "Featured
repos" table before any runnable instruction. The only CLI invocation,
`node cli.mjs search synthetic payment`, sat at line 76 inside "Explore
further". Inserted a `## Quick start` section right after the intro paragraph
using the public commands from `package.json` (`node cli.mjs validate`,
`node --test`, `npm run check`) and a Node 22+ constraint pulled from
`package.json` `engines.node`.
